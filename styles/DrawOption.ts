import {StyleSheet} from "react-native";
export const styles = StyleSheet.create({
    canvasDrawOption: {
        flex: 1,
        padding: 10,
        alignItems: 'center',
        justifyContent: 'space-evenly',
    },
});